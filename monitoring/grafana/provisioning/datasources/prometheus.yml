apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: rate-limiter-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      # Matches prometheus.yml, so Grafana picks sensible step sizes.
      timeInterval: 5s
